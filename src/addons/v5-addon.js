// V5: every-country city atlas, local safety, commodity trade and national accounts.
const CITY_CATALOG=JSON.parse($('city-catalog').textContent),ADDED_COUNTRIES=JSON.parse($('added-countries').textContent),ADDED_IDS=new Set(ADDED_COUNTRIES.map(n=>n.id));
const SMALL_BASELINES={AND:[.08,4],ATG:[.1,2.3],BHR:[1.6,48],BRB:[.28,7],CPV:[.6,3],COM:[.9,1.5],DMA:[.075,.7],GRD:[.12,1.4],KIR:[.13,.35],LIE:[.04,7.5],MDV:[.53,7],MHL:[.06,.3],MLT:[.56,25],MUS:[1.3,17],FSM:[.11,.5],MCO:[.039,10],NRU:[.013,.16],PLW:[.018,.35],SMR:[.034,2.2],STP:[.24,.8],SGP:[6,560],TON:[.105,.6],TUV:[.011,.07],VCT:[.11,1.2],VAT:[.001,.06],SYC:[.12,2.2],KNA:[.055,1.1],LCA:[.18,2.8],WSM:[.225,1.1]};
for(const c of ADDED_COUNTRIES){const[pop,gdp]=SMALL_BASELINES[c.id]||[.2,2];if(!SEEDS.some(n=>n[0]===c.id))SEEDS.push([c.id,c.name,pop,gdp,55,2.5,3,75,65,'Social liberal',c.lon,c.lat,c.flag||'🌐']);}
const V5_POLICIES=[
{key:'tariff',name:'Import tariff',min:0,max:40,step:1,unit:'%',group:'Trade & investment',desc:'Tax on delivered imports. Raises customs revenue and input costs. Free-trade agreements waive this duty.'},
{key:'corporateSurcharge',name:'Corporate profit surcharge',min:0,max:20,step:1,unit:'%',desc:'An additional levy on a modeled corporate-profit base of 20% of GDP. Separate from existing aggregate taxes.'},
{key:'capitalControls',name:'Capital-flow restrictions',min:0,max:100,step:5,unit:'/ 100',desc:'Reduces exposure to external credit shocks, while constraining some private investment.'},
{key:'competition',name:'Competition enforcement',min:0,max:100,step:5,unit:'/ 100',desc:'More competitive markets support productivity over time. Costs up to 0.3% of GDP annually.'},
{key:'publicEmployment',name:'Public employment program',min:0,max:5,step:.25,unit:'% GDP',desc:'Funds public jobs and reduces unemployment. The wage bill recurs each year.'},
{key:'smeCredit',name:'Small-business grants',min:0,max:3,step:.25,unit:'% GDP',desc:'Budget-funded grants support small firms, jobs and investment.'},
{key:'justice',name:'Courts & justice capacity',min:0,max:100,step:5,unit:'/ 100',group:'Public safety',desc:'Judicial capacity complements prevention and accountable policing. Costs up to 0.8% of GDP annually.'},
{key:'armyShare',name:'Army budget allocation',min:0,max:100,step:5,unit:'% defense',group:'Defense planning',military:true,desc:'Share of the defense budget directed to land forces. Army, navy and air allocations must total 100%.'},
{key:'navyShare',name:'Navy budget allocation',min:0,max:100,step:5,unit:'% defense',military:true,desc:'Share directed to naval capability. This redistributes the existing defense budget.'},
{key:'airShare',name:'Air-force budget allocation',min:0,max:100,step:5,unit:'% defense',military:true,desc:'Share directed to aviation. This redistributes the existing defense budget.'},
{key:'training',name:'Training intensity',min:0,max:100,step:5,unit:'/ 100',military:true,desc:'Improves readiness, but dedicates a larger part of defense resources to training instead of equipment.'},
{key:'logistics',name:'Logistics & maintenance priority',min:0,max:100,step:5,unit:'/ 100',military:true,desc:'Improves effective capability. More maintenance leaves less of the defense budget for procurement.'},
{key:'conscription',name:'Service & reserve commitment',min:0,max:100,step:5,unit:'/ 100',military:true,desc:'Expands the personnel pool. High commitments remove workers from civilian production.'},
{key:'mobilization',name:'Reserve mobilization',min:0,max:100,step:5,unit:'/ 100',military:true,desc:'More personnel on active duty. Costs up to 1.5% of GDP beyond the regular defense budget and disrupts civilian output.'}
];
const V5_DEFAULTS={tariff:5,corporateSurcharge:0,capitalControls:25,competition:50,publicEmployment:0,smeCredit:0,justice:50,armyShare:50,navyShare:25,airShare:25,training:55,logistics:55,conscription:15,mobilization:0};
POLICY.push(...V5_POLICIES);Object.assign(EXTRA_DEFAULTS,V5_DEFAULTS);for(const p of Object.values(PRESETS))Object.assign(p,V5_DEFAULTS);IDEOLOGY_EXEMPT.push(...V5_POLICIES.filter(p=>p.military).map(p=>p.key),'justice');
Object.assign(PROJECTS,{police:{name:'Community safety precinct',cost:.5,months:12,desc:'Accountable local policing reduces crime pressure gradually.',effects:{quality:4}},courts:{name:'Courts & legal-access center',cost:.6,months:18,desc:'Improves justice capacity and reduces unresolved crime pressure.',effects:{quality:4,education:2}},youth:{name:'Youth services & employment center',cost:.5,months:12,desc:'Prevention, learning and opportunities improve safety and local jobs.',effects:{education:6,jobs:5,quality:4}},waste:{name:'Waste & flood-resilience network',cost:.7,months:16,desc:'Cleaner districts and more resilient essential infrastructure.',effects:{health:7,quality:7}},freight:{name:'Freight & distribution terminal',cost:1.1,months:20,desc:'Improves logistics and the ability to deliver traded goods.',effects:{mobility:8,jobs:8}}});
PROGRAMS.push({key:'safety',name:'Safer communities',tag:'Cities & justice',blurb:'Combine national courts with city prevention, policing and local services. More policing alone is not an instant cure.',keys:['justice','antiCorruption','publicEmployment','services'],stage:{justice:80,antiCorruption:80,publicEmployment:1.5,services:13},stageNote:'Stages courts, oversight, public jobs and services. Set each city’s prevention and policing budgets in Cities.'},{key:'enterprise',name:'Trade & small enterprise',tag:'Economic management',blurb:'Trade routes need productive firms, competition and workable border costs.',keys:['tariff','trade','competition','smeCredit','capitalControls'],stage:{tariff:5,trade:85,competition:80,smeCredit:1.5,capitalControls:20},stageNote:'Stages open trade, competition enforcement and small-business support.'});
const LOCAL_RULES=[{key:'police',name:'Accountable policing',min:0,max:100,step:5},{key:'prevention',name:'Prevention & youth services',min:0,max:100,step:5},{key:'services',name:'Local health & social services',min:0,max:100,step:5},{key:'maintenance',name:'Municipal maintenance',min:0,max:100,step:5},{key:'density',name:'Housing density & permitting',min:0,max:100,step:5},{key:'tax',name:'Local tax supplement · % local GDP',min:0,max:10,step:.5}];
const LOCAL_DEFAULTS={police:50,prevention:45,services:50,maintenance:55,density:55,tax:2};
const GOODS={food:{name:'Food & agriculture',capacity:.07},energy:{name:'Energy & fuels',capacity:.07},manufactures:{name:'Manufactured goods',capacity:.11},technology:{name:'Technology & equipment',capacity:.07}};
const DOCTRINES={balanced:'Balanced forces',defensive:'Territorial defense',expeditionary:'Expeditionary logistics',deterrence:'Readiness & deterrence'};
let cityDraft=null,cityDraftId=null,tradePartner=null;
function cityKey(name){return name.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/\bcity\b/g,'').replace(/[^a-z0-9]/g,'');}
function cityTemplate(n){const entry=CITY_CATALOG[n.id];if(!entry?.cities.length)throw Error('Missing city catalogue for '+n.id);const weights=entry.cities.map((_,i)=>1/Math.pow(i+2,.7)),sum=weights.reduce((a,b)=>a+b,0),coverage=weights.length===1?1:.75;return entry.cities.map((name,i)=>({id:n.id+'-'+i,name,share:weights[i]/sum*coverage,levels:Object.fromEntries(Object.keys(PROJECTS).map(k=>[k,0])),queue:[],management:{...LOCAL_DEFAULTS},crime:clamp(18+n.inequality*.4+n.slums*.15+i*1.5,10,75),satisfaction:60}));}
function upgradeNationV5(n){for(const k of ['policies','targets'])n[k]={...V5_DEFAULTS,...n[k]};const old=n.cities||[],used=new Set();n.cities=cityTemplate(n).map((c,i)=>{let previous=old.find(x=>!used.has(x)&&cityKey(x.name)===cityKey(c.name));if(!previous)previous=old.find(x=>!used.has(x)&&['Capital district','Regional center','Growth district'].includes(x.name));if(previous){used.add(previous);Object.assign(c.levels,previous.levels);c.queue=previous.queue.map(q=>({...q,paused:false}));}return c;});n.identity={mode:'national',flagId:n.id,pattern:'horizontal',colors:['#173d5c','#f4eee0','#b04444']};n.accounting={materialShare:clamp(.62-Math.log10(Math.max(500,n.gdp/n.pop*1000))*.04,.32,.6),depreciation:8};n.forces={army:n.military*.5,navy:n.military*.25,air:n.military*.25};n.doctrine='balanced';n.exports=0;n.imports=0;n.customs=0;n.tradeGrowth=0;n.cityOperating=0;n.cityRevenue=0;n.crime=40;n.citySatisfaction=60;}
const v4NewGameV5=newGame;
newGame=function(){v4NewGameV5();S.version=5;S.options.outputMeasure='gdp';S.options.monthSeconds=8;S.tradeDeals=[];S.tradeFlows=[];S.nextTradeId=1;for(const n of S.nations)upgradeNationV5(n);activeCity=0;cityDraft=null;cityDraftId=null;tradePartner=null;addEvent('A larger living world','Every country now has named cities or settlements. Local safety, bilateral commodity trade and separate armed services are available.','dispatch');};
function outputValue(n,mode=S.options.outputMeasure){return mode==='nmp'?Math.max(0,n.gdp*n.accounting.materialShare*(1-n.accounting.depreciation/100)):n.gdp;}
function outputName(){return S.options.outputMeasure==='nmp'?'Net material product':'Gross domestic product';}
function changeOutput(mode){if(!['gdp','nmp'].includes(mode))return;S.options.outputMeasure=mode;renderStats();renderPanel();toast(mode==='nmp'?'Displaying modeled NMP. GDP remains the fiscal base.':'Displaying GDP.');}
function flagHtml(n){const id=n.identity;if(!id||id.mode==='national')return '<span class="flag">'+escapeHTML(CITY_CATALOG[n.id]?.flag||n.flag)+'</span>';if(id.mode==='country')return '<span class="flag">'+escapeHTML(CITY_CATALOG[id.flagId]?.flag||'🌐')+'</span>';const angle=id.pattern==='vertical'?'90deg':'180deg',colors=id.colors;return '<span class="customflag" role="img" aria-label="Custom three-color national flag" style="background:linear-gradient('+angle+','+colors[0]+' 0% 33.333%,'+colors[1]+' 33.333% 66.666%,'+colors[2]+' 66.666% 100%)"></span>';}
const v4ProvenanceV5=provenance;
provenance=function(n){return ADDED_IDS.has(n.id)?'Expanded-country scenario. Population, output and institutions are rounded gameplay baselines, not a live statistical dataset.':v4ProvenanceV5(n);};
function activeProject(c){return c.queue.find(q=>!q.paused);}
const v4PrepareV5=prepareV4Month;
prepareV4Month=function(){v4PrepareV5();for(const n of S.nations){n.citySpend=0;n.cityOperating=0;n.cityRevenue=0;for(const c of n.cities){const q=activeProject(c);if(q)n.citySpend+=q.cost/q.duration*1200/n.gdp;const m=c.management;n.cityOperating+=c.share*(m.police*.012+m.prevention*.008+m.services*.016+m.maintenance*.01);n.cityRevenue+=c.share*m.tax;}}prepareTrade();};
const v4MetricsV5=cityMetrics;
cityMetrics=function(n,c){const m=v4MetricsV5(n,c),p=c.management;m.health=clamp(m.health+(p.services-50)*.15,0,100);m.housing=clamp(m.housing+(p.density-50)*.14,0,100);m.jobs=clamp(m.jobs-(p.tax-2)*1.1,0,100);m.quality=clamp(m.quality+(p.maintenance-50)*.15-(c.crime-30)*.12,0,100);return m;};
tickCities=function(n){let crime=0,satisfaction=0,shares=0;for(const c of n.cities){const q=activeProject(c);if(q){q.remaining--;if(q.remaining<=0){c.levels[q.type]++;c.queue=c.queue.filter(x=>x!==q);if(n.id===S.player)addEvent('City project completed',c.name+': '+PROJECTS[q.type].name+' is now operating.','development');}}const m=cityMetrics(n,c),local=c.management;const target=clamp(19+n.unemployment*.8+n.inequality*.35+(60-m.housing)*.15+(65-m.jobs)*.12+(100-n.capacity)*.08-local.police*.12-local.prevention*.15-n.policies.justice*.1-c.levels.police*1.5-c.levels.courts*2-c.levels.youth*2,3,95);c.crime+=(target-c.crime)/18;const support=clamp(30+m.quality*.3+m.health*.2+m.jobs*.15+(100-c.crime)*.15-local.tax*1.2,5,98);c.satisfaction+=(support-c.satisfaction)/12;crime+=c.crime*c.share;satisfaction+=c.satisfaction*c.share;shares+=c.share;n.human+=(m.education+m.health-100)*c.share*.0005;n.clean+=c.levels.solar*c.share*.015;n.rentBurden=clamp(n.rentBurden-c.levels.housing*c.share*.01,8,65);}n.crime=crime/shares;n.citySatisfaction=satisfaction/shares;n.approval=clamp(n.approval+(n.citySatisfaction-n.approval)*.003,5,95);if(S.options.ai&&n.id!==S.player&&S.month%12===0){const c=n.cities[Math.floor(S.month/12)%n.cities.length],keys=Object.keys(PROJECTS);startCityProject(n.id,c.id,keys[(Math.floor(S.month/12)+n.id.charCodeAt(0))%keys.length],true);}};
function projectMonths(n,c,p){return Math.max(6,Math.ceil(p.months*(1.12-n.policies.decentralization*.0015-c.management.maintenance*.001)));}
startCityProject=function(nid,cid,type,ai=false){const n=nation(nid),c=n?.cities.find(c=>c.id===cid),p=PROJECTS[type];if(!n||!c||!p||(!ai&&n.id!==S.player))return false;if(c.queue.length>=3||c.levels[type]+c.queue.filter(q=>q.type===type).length>=5){if(!ai)toast('The city queue is full, or this development has reached its five-level limit.');return false;}if(!canSpend(n,6)){if(!ai)toast('This project requires 6 political capital.');return false;}spendCapital(n,6);const duration=projectMonths(n,c,p);c.queue.push({type,duration,remaining:duration,cost:n.gdp*c.share*p.cost/100,paused:false});if(!ai){addEvent('City development approved',c.name+' queued '+p.name.toLowerCase()+'. One unpaused project is financed and built at a time.','development');renderPanel();renderStats();toast('Project added to the city queue.');}return true;};
function queueAction(action,index){const n=current(),c=n.cities[activeCity];if(n.id!==S.player||!Number.isInteger(index)||!c.queue[index])return;const q=c.queue[index];if(action==='pause')q.paused=!q.paused;if(action==='priority'){c.queue.splice(index,1);c.queue.unshift(q);}if(action==='cancel')c.queue.splice(index,1);renderPanel();toast(action==='cancel'?'Future project payments cancelled; past spending is not refunded.':'Construction queue updated.');}
function applyCityManagement(){const n=current(),c=n.cities[activeCity];if(n.id!==S.player||!cityDraft||cityDraftId!==c.id)return;for(const x of LOCAL_RULES)if(!Number.isFinite(cityDraft[x.key])||cityDraft[x.key]<x.min||cityDraft[x.key]>x.max){toast('Local settings are outside their limits.');return;}if(!canSpend(n,4)){toast('This change needs 4 political capital.');return;}spendCapital(n,4);c.management={...cityDraft};renderPanel();renderStats();toast('Local management updated. Safety and satisfaction change gradually.');}
function crimeLabel(value){return value<25?'Low pressure':value<45?'Moderate pressure':value<65?'High pressure':'Severe pressure';}
function localBudget(c){const m=c.management;return{spending:m.police*.012+m.prevention*.008+m.services*.016+m.maintenance*.01,revenue:m.tax};}
function citiesV5Panel(n){activeCity=clamp(activeCity,0,n.cities.length-1);const c=n.cities[activeCity],m=cityMetrics(n,c),owned=n.id===S.player,entry=CITY_CATALOG[n.id];if(cityDraftId!==c.id){cityDraft={...c.management};cityDraftId=c.id;}const running=activeProject(c),budget=localBudget(c);return '<h3>Cities & local government</h3><label class="eyebrow" for="citySelect">'+n.cities.length+' named '+escapeHTML(entry.kind||'cities and settlements')+'</label><select class="full" id="citySelect">'+n.cities.map((x,i)=>'<option value="'+i+'" '+(i===activeCity?'selected':'')+'>'+escapeHTML(x.name)+' · crime '+x.crime.toFixed(0)+'</option>').join('')+'</select><div class="ideologybox" style="margin-top:18px"><div class="eyebrow">Municipal simulation</div><strong>'+escapeHTML(c.name)+'</strong>'+metric('Local output · GDP',money(n.gdp*c.share))+metric('Resident population · scenario',(n.pop*c.share).toFixed(3)+'M')+metric('Public satisfaction',c.satisfaction.toFixed(0)+' / 100')+'</div><div class="crimecard"><div class="eyebrow">Simulated crime pressure</div><strong>'+c.crime.toFixed(1)+' <small>/ 100</small></strong><span>'+crimeLabel(c.crime)+'</span><p>Higher is worse. This is a game index, not a measured crime rate or real-world ranking.</p></div><div class="previewgrid">'+Object.entries(m).map(([k,v])=>'<div class="minicard"><label>'+k[0].toUpperCase()+k.slice(1)+'</label><strong>'+v.toFixed(0)+'<small style="font-size:12px"> /100</small></strong></div>').join('')+'<details class="controlgroup" open><summary>Local management</summary><p>Local taxes and operating budgets are additional to national programs. Current local balance: '+pct(budget.revenue-budget.spending)+' of local GDP per year.</p>'+LOCAL_RULES.map(x=>'<div class="policy"><label class="metricrow" for="local-'+x.key+'"><span>'+x.name+'</span><output id="lo-'+x.key+'">'+cityDraft[x.key]+'</output></label><input type="range" id="local-'+x.key+'" data-local="'+x.key+'" min="'+x.min+'" max="'+x.max+'" step="'+x.step+'" value="'+cityDraft[x.key]+'" '+(!owned?'disabled':'')+'></div>').join('')+'<button class="primary full" id="applyLocal" '+(!owned||!canSpend(n,4)?'disabled':'')+'>Apply local management · 4 capital</button></details><h3>Construction queue · '+c.queue.length+'/3</h3><p>One unpaused project runs at a time. Reorder the queue to change priority.</p>'+(c.queue.length?c.queue.map((q,i)=>'<div class="projectcard"><div class="eyebrow">'+(q.paused?'Paused':q===running?'Building now':'Queued')+'</div><strong>'+PROJECTS[q.type].name+'</strong>'+metric('Work remaining',q.remaining+' months')+progressBar((1-q.remaining/q.duration)*100)+metric('Remaining finance',money(q.cost*q.remaining/q.duration))+(owned?'<div class="buttonrow"><button data-queue="pause" data-index="'+i+'">'+(q.paused?'Resume':'Pause')+'</button><button data-queue="priority" data-index="'+i+'">Prioritize</button><button data-queue="cancel" data-index="'+i+'">Cancel</button></div>':'')+'</div>').join(''):'<p>No projects queued.</p>')+'<details class="controlgroup"><summary>Approve a new development · '+Object.keys(PROJECTS).length+' types</summary>'+Object.entries(PROJECTS).map(([k,p])=>'<div class="projectcard"><strong>'+p.name+'</strong>'+metric('Level',c.levels[k]+' / 5')+'<p>'+p.desc+'</p>'+metric('Total finance',money(n.gdp*c.share*p.cost/100))+metric('Active construction time',projectMonths(n,c,p)+' months')+'<button class="full" data-build="'+k+'" '+(!owned||!canSpend(n,6)||c.queue.length>=3||c.levels[k]+c.queue.filter(q=>q.type===k).length>=5?'disabled':'')+'>Queue development · 6 capital</button></div>').join('')+'</details><p class="note">'+escapeHTML(entry.note||'Place names are real; municipal populations, output shares, crime and services are scenario assumptions.')+' The listed settlements account for '+(n.cities.length===1?'100':'75')+'% of modeled output; the remainder stays in the national economy.</p>';}

function exportCapacity(n,good){const salt=[...n.id].reduce((a,c)=>a+c.charCodeAt(0),0),specialty=Object.keys(GOODS)[salt%4],factor=good===specialty?1.4:.75,industrial=good==='manufactures'?1+n.policies.industrial*.06:good==='technology'?1+n.policies.research*.08:1;return n.gdp*GOODS[good].capacity*factor*industrial/12;}
function tradeProblem(from,to,good,share){if(!from||!to||from.id===to.id||!GOODS[good])return 'Select two different countries and a valid commodity.';if(!Number.isFinite(share)||share<.25||share>5)return 'The contract must be 0.25–5% of exporter GDP per year.';if(from.war?.target===to.id||S.sanctions.includes(pair(from.id,to.id)))return 'War or sanctions block this trade route.';if(relation(from.id,to.id)<0)return 'Trade requires nonnegative bilateral relations.';if(Math.min(from.policies.trade,to.policies.trade)<20)return 'Both nations need trade openness of at least 20.';if(S.tradeDeals.some(d=>d.from===from.id&&d.to===to.id&&d.good===good))return 'This route already has a contract for that commodity.';if(S.tradeDeals.length>=1000)return 'The world trade-contract limit has been reached.';return '';}
function signTrade(partnerId,direction,good,share,ai=false,aiFrom=null){const a=ai?aiFrom:player(),b=nation(partnerId);if(!a||!b||!['export','import'].includes(direction))return false;const from=direction==='export'?a:b,to=direction==='export'?b:a,issue=tradeProblem(from,to,good,share);if(issue){if(!ai)toast(issue);return false;}if(!canSpend(a,10)){if(!ai)toast('A trade contract needs 10 political capital.');return false;}spendCapital(a,10);S.tradeDeals.push({id:S.nextTradeId++,from:from.id,to:to.id,good,share,expires:S.month+60});if(!ai){addEvent('Commodity contract signed',from.name+' will export '+GOODS[good].name.toLowerCase()+' to '+to.name+'. Deliveries start next month.','trade');renderPanel();renderStats();toast('Five-year trade contract signed.');}return true;}
function cancelTrade(id){const d=S.tradeDeals.find(d=>d.id===id);if(!d||![d.from,d.to].includes(S.player))return;S.tradeDeals=S.tradeDeals.filter(x=>x!==d);setRelation(d.from,d.to,relation(d.from,d.to)-3);renderPanel();toast('Future deliveries cancelled. Relations fall by 3.');}
function prepareTrade(){const expired=S.tradeDeals.filter(d=>d.expires<=S.month);S.tradeDeals=S.tradeDeals.filter(d=>d.expires>S.month);for(const d of expired)if([d.from,d.to].includes(S.player))addEvent('Trade contract expired',GOODS[d.good].name+' contract between '+nation(d.from).name+' and '+nation(d.to).name+' needs renewal.','trade');for(const n of S.nations){n.exports=0;n.imports=0;n.customs=0;n.tradeGrowth=0;}const requests=[],byExporter=new Map(),byImporter=new Map();for(const d of S.tradeDeals){const from=nation(d.from),to=nation(d.to);const blocked=from.war?.target===to.id||S.sanctions.includes(pair(from.id,to.id))||Math.min(from.policies.trade,to.policies.trade)<20;const tariff=hasTreaty(from.id,to.id)?0:to.policies.tariff/100,wanted=blocked?0:from.gdp*d.share/1200/(1+tariff),key=from.id+':'+d.good;requests.push({d,from,to,tariff,wanted,key,blocked});byExporter.set(key,(byExporter.get(key)||0)+wanted);byImporter.set(to.id,(byImporter.get(to.id)||0)+wanted);}S.tradeFlows=[];for(const r of requests){const exportLimit=exportCapacity(r.from,r.d.good),importLimit=r.to.gdp*.25/12,ratio=Math.min(1,exportLimit/Math.max(.0000001,byExporter.get(r.key)),importLimit/Math.max(.0000001,byImporter.get(r.to.id))),value=r.wanted*ratio,duty=value*r.tariff;r.from.exports+=value;r.to.imports+=value;r.to.customs+=duty;S.tradeFlows.push({id:r.d.id,value,duty,blocked:r.blocked});}for(const n of S.nations)n.tradeGrowth=clamp((n.exports+n.imports)/n.gdp*12*2,0,1.2);}
function tradeSummary(n){return '<div class="previewgrid"><div class="minicard"><label>Exports · last month</label><strong>'+money(n.exports)+'</strong></div><div class="minicard"><label>Imports · last month</label><strong>'+money(n.imports)+'</strong></div></div>'+metric('Trade balance · last month',money(n.exports-n.imports))+metric('Customs revenue · last month',money(n.customs))+'<p>Exports and imports are matched at the same pre-tariff value. Private export revenue does not become government money. Only customs duties enter the public budget.</p>';}
function tradePanel(){const a=player();if(!tradePartner||tradePartner===a.id||!nation(tradePartner))tradePartner=selected!==a.id?selected:S.nations.find(n=>n.id!==a.id).id;const deals=S.tradeDeals.filter(d=>[d.from,d.to].includes(a.id));return '<h3>International commodity trade</h3><div id="tradeSummary">'+tradeSummary(a)+'</div><label class="eyebrow" for="tradePartner">Partner nation</label><select class="full" id="tradePartner">'+S.nations.filter(n=>n.id!==a.id).sort((a,b)=>a.name.localeCompare(b.name)).map(n=>'<option value="'+n.id+'" '+(n.id===tradePartner?'selected':'')+'>'+escapeHTML(n.name)+'</option>').join('')+'</select><div class="previewgrid"><label>Direction<select class="full" id="tradeDirection"><option value="export">Export to partner</option><option value="import">Import from partner</option></select></label><label>Commodity<select class="full" id="tradeGood">'+Object.entries(GOODS).map(([k,g])=>'<option value="'+k+'">'+g.name+'</option>').join('')+'</select></label></div><label class="metricrow" for="tradeShare"><span>Annual size · % exporter GDP</span><input id="tradeShare" type="number" min=".25" max="5" step=".25" value="1"></label><button class="primary full" id="signTrade">Sign five-year contract · 10 capital</button><p>Contracts require nonnegative relations, openness of at least 20, and no bilateral war or sanctions. Deliveries are capped by exporter capacity and importer demand, and tariffs reduce trade. Free-trade agreements waive tariffs.</p><h3>Active contracts · '+deals.length+'</h3>'+(deals.length?deals.map(d=>{const flow=S.tradeFlows.find(f=>f.id===d.id);return '<div class="projectcard"><div class="eyebrow">'+GOODS[d.good].name+'</div><strong>'+escapeHTML(nation(d.from).name)+' → '+escapeHTML(nation(d.to).name)+'</strong>'+metric('Annual contract target',d.share+'% exporter GDP')+metric('Last month delivered',flow?money(flow.value):'Starts next month')+metric('Status',flow?.blocked?'Suspended by restrictions':'Trading / awaiting next month')+metric('Remaining term',d.expires-S.month+' months')+'<button class="full" data-cancel-trade="'+d.id+'">Cancel future deliveries · relations −3</button></div>';}).join(''):'<p>No commodity contracts yet. Diplomatic trade agreements and delivery contracts are separate.</p>')+'<p class="note">Commodity capacity and national specializations are scenario assumptions. International trade always matches seller and buyer values; it is not an unlimited-money action.</p>';}
const v4TickDiplomacyV5=tickDiplomacy;
tickDiplomacy=function(){v4TickDiplomacyV5();if(S.options.ai&&S.month%6===0){const pool=S.nations.filter(n=>n.id!==S.player&&!n.war);for(let i=0;i<3&&pool.length>1;i++){const a=pool[Math.floor(random()*pool.length)],b=pool[Math.floor(random()*pool.length)];if(a!==b)signTrade(b.id,'export',Object.keys(GOODS)[Math.floor(random()*4)],1,true,a);}}};
const v4FiscalV5=fiscal;
fiscal=function(n,p=n.policies){const b=v4FiscalV5(n,p),eff=.75+n.capacity*.0025;const corporate=p.corporateSurcharge*.2*eff,local=n.cityRevenue||0,customs=p.tariff===n.policies.tariff?(n.customs||0)/n.gdp*1200:(n.customs||0)/Math.max(.000001,n.policies.tariff)*p.tariff/n.gdp*1200;const additionalSpend=(n.cityOperating||0)+p.publicEmployment+p.smeCredit+p.justice*.008+p.competition*.003+p.mobilization*.015;b.revenue+=corporate+local+customs;b.spending+=additionalSpend;b.balance=b.revenue-b.spending-b.debtService;b.v5Revenue={corporate,local,customs};b.v5Spending=additionalSpend;return b;};
function militaryProblem(p){return Math.abs(p.armyShare+p.navyShare+p.airShare-100)>.001?'Army, navy and air-force allocations must total 100%.':'';}
const v4TaxProblemV5=taxProblem;
taxProblem=function(p){return v4TaxProblemV5(p)||militaryProblem(p);};
const v4TickNationV5=tickV4Nation;
tickV4Nation=function(n){v4TickNationV5(n);const p=n.policies,resources=n.gdp*p.defense/100*3*(.6+n.capacity*.004),equipmentBudget=1-p.training*.0018-p.logistics*.001;for(const[k,share]of [['army','armyShare'],['navy','navyShare'],['air','airShare']])n.forces[k]=Math.max(0,n.forces[k]+(resources*p[share]/100*equipmentBudget-n.forces[k])/36);n.military=n.forces.army+n.forces.navy+n.forces.air;n.readiness=clamp(n.readiness+((35+p.training*.5+p.logistics*.2)-n.readiness)/24,20,95);n.unemployment=clamp(n.unemployment-p.publicEmployment*.02-p.smeCredit*.008,2,28);const shareTarget=clamp(.50+p.industrial*.012+p.foodSecurity*.005-p.research*.005,.32,.68);n.accounting.materialShare+=(shareTarget-n.accounting.materialShare)/120;n.approval=clamp(n.approval-(n.crime-35)*.001,5,95);};
function forceStrength(n){let modifier=.7+n.policies.logistics*.004+n.policies.mobilization*.001;if(n.doctrine==='defensive')modifier*=n.war?.aggressor?.94:1.10;if(n.doctrine==='expeditionary')modifier*=n.war?.aggressor?1.08:.96;if(n.doctrine==='deterrence')modifier*=1+n.policies.training*.0005;return Math.max(1,n.military*n.readiness/100*modifier);}
const v4DeclareWarV5=declareWar;
declareWar=function(){const a=player(),b=current();v4DeclareWarV5();if(a.war?.target===b.id){a.war.aggressor=true;b.war.aggressor=false;}};
function defenseControls(n){if(!draft)draft={...n.targets};const problem=militaryProblem(draft);return '<details class="controlgroup" open><summary>Armed forces & doctrine</summary>'+metric('Army equipment index',n.forces.army.toFixed(0))+metric('Navy equipment index',n.forces.navy.toFixed(0))+metric('Air-force equipment index',n.forces.air.toFixed(0))+metric('Modeled service personnel',(n.pop*(.002+n.policies.conscription*.00004+n.policies.mobilization*.00002)*1000).toFixed(0)+' thousand')+'<label for="doctrine" class="eyebrow">Strategic doctrine</label><select id="doctrine" class="full" '+(n.id!==S.player?'disabled':'')+'>'+Object.entries(DOCTRINES).map(([k,v])=>'<option value="'+k+'" '+(n.doctrine===k?'selected':'')+'>'+v+'</option>').join('')+'</select><p>Doctrine changes take effect immediately. Equipment adjusts over years; training affects readiness. Mobilization adds spending and reduces civilian output.</p></details><details class="controlgroup"><summary>Allocate defense resources & enact reform</summary>'+POLICY.filter(x=>x.military||x.key==='defense').map(x=>policyControl({...x,group:null},n)).join('')+'<div id="defenseAllocation" class="note '+(problem?'negative':'')+'">Allocation total: '+(draft.armyShare+draft.navyShare+draft.airShare).toFixed(0)+'%'+(problem?' · '+problem:'')+'</div>'+reformFooter(n)+'</details>';}
const v4MilitaryPanelV5=militaryPanel;
militaryPanel=function(n){return defenseControls(n)+v4MilitaryPanelV5(n);};
const v4EconomyPanelV5=economyPanel;
economyPanel=function(n){const b=fiscal(n);return '<div class="block"><h3>National accounts</h3><label class="eyebrow" for="outputMeasure">Display output as</label><select id="outputMeasure" class="full"><option value="gdp" '+(S.options.outputMeasure==='gdp'?'selected':'')+'>GDP · Gross Domestic Product</option><option value="nmp" '+(S.options.outputMeasure==='nmp'?'selected':'')+'>NMP · Net Material Product</option></select>'+metric('GDP · gross domestic product',money(n.gdp))+metric('NMP · modeled estimate',money(outputValue(n,'nmp')))+metric('Modeled material-production share',(n.accounting.materialShare*100).toFixed(1)+'% GDP')+metric('Depreciation of material output',n.accounting.depreciation.toFixed(1)+'%')+'<p>NMP here is material-sector value added minus material capital consumption. It excludes most nonmaterial services. This approximate historical measure is not simply GDP minus depreciation. Budgets and debt ratios always use GDP.</p></div><div class="block"><h3>Local government & new controls</h3>'+metric('Local operating spending',(n.cityOperating||0).toFixed(2)+'% GDP')+metric('Local tax supplements',b.v5Revenue.local.toFixed(2)+'% GDP')+metric('Corporate surcharge revenue',b.v5Revenue.corporate.toFixed(2)+'% GDP')+metric('Customs revenue',b.v5Revenue.customs.toFixed(2)+'% GDP')+'<button class="full" data-action="tradeView">Manage commodity trade →</button><button class="full" data-action="economyControls">Open economic controls →</button></div>'+v4EconomyPanelV5(n);};
const v4RenderStatsV5=renderStats;
renderStats=function(){v4RenderStatsV5();const n=player();$('stats').innerHTML=$('stats').innerHTML.replace('<label>National output</label>','<label>'+(S.options.outputMeasure==='nmp'?'NMP · modeled estimate':'GDP · national output')+'</label>').replace(money(n.gdp),money(outputValue(n)));};
const v4UpdatePreviewV5=updateReformPreview;
updateReformPreview=function(){v4UpdatePreviewV5();if($('defenseAllocation')&&draft){const total=draft.armyShare+draft.navyShare+draft.airShare;$('defenseAllocation').textContent='Allocation total: '+total.toFixed(0)+'%'+(militaryProblem(draft)?' · Must total 100%.':'');}};
const v4PoliciesPanelV5=policiesPanel;
policiesPanel=function(n){if(subview==='economicControls'){if(!draft)draft={...n.targets};return '<h3>Economic management</h3><p>Stage fiscal, industrial and market reforms. Review the combined budget impact before enacting.</p>'+POLICY.filter(x=>['tariff','corporateSurcharge','capitalControls','competition','publicEmployment','smeCredit','rate','industrial','research','trade'].includes(x.key)).map(x=>policyControl(x,n)).join('')+reformFooter(n);}return v4PoliciesPanelV5(n);};
Object.assign(actions,{tradeView:()=>setView('trade'),economyControls:()=>{view='policies';subview='economicControls';draft={...current().targets};renderHead();renderPanel();},identity:()=>identityModal()});
function identityModal(){const n=player(),id=n.identity;modal('National flag & identity','<p>Change the displayed flag while keeping the same country, borders and saved economy.</p><label for="flagMode" class="eyebrow">Flag style</label><select id="flagMode" class="full"><option value="national" '+(id.mode==='national'?'selected':'')+'>Original national flag</option><option value="country" '+(id.mode==='country'?'selected':'')+'>Use another national flag</option><option value="custom" '+(id.mode==='custom'?'selected':'')+'>Custom three-color flag</option></select><label for="flagCountry" class="eyebrow">National flag to use</label><select class="full" id="flagCountry">'+S.nations.filter(n=>CITY_CATALOG[n.id]?.flag).sort((a,b)=>a.name.localeCompare(b.name)).map(n=>'<option value="'+n.id+'" '+(id.flagId===n.id?'selected':'')+'>'+escapeHTML(CITY_CATALOG[n.id].flag+' '+n.name)+'</option>').join('')+'</select><label for="flagPattern" class="eyebrow">Custom stripe direction</label><select class="full" id="flagPattern"><option value="horizontal" '+(id.pattern==='horizontal'?'selected':'')+'>Horizontal</option><option value="vertical" '+(id.pattern==='vertical'?'selected':'')+'>Vertical</option></select><div class="buttonrow" style="margin:18px 0">'+id.colors.map((color,i)=>'<label>Color '+(i+1)+' <input type="color" id="flagColor'+i+'" value="'+color+'"></label>').join('')+'</div><button class="primary full" id="saveFlag">Apply flag</button>');$('saveFlag').onclick=()=>{const next={mode:$('flagMode').value,flagId:$('flagCountry').value,pattern:$('flagPattern').value,colors:[0,1,2].map(i=>$('flagColor'+i).value)};if(!validIdentity(next))return;n.identity=next;closeModal();render();toast('National flag updated.');};}
function validIdentity(x){return !!x&&['national','country','custom'].includes(x.mode)&&!!CITY_CATALOG[x.flagId]&&['horizontal','vertical'].includes(x.pattern)&&Array.isArray(x.colors)&&x.colors.length===3&&x.colors.every(c=>/^#[0-9a-f]{6}$/i.test(c));}
const v4SettingsV5=settingsPanel;
settingsPanel=function(){return '<h3>Time & national identity</h3><label class="metricrow" for="monthSeconds"><span>Seconds per month at 1×</span><input type="number" id="monthSeconds" min="4" max="30" step="1" value="'+S.options.monthSeconds+'"></label><p>Default 1× speed is one month every eight seconds. The 3× and 12× speeds scale from this setting.</p><button class="full" data-action="identity">Change your national flag</button><div class="block"></div>'+v4SettingsV5();};
const v4BindV5=bindPanel;
bindPanel=function(){v4BindV5();if($('citySelect'))$('citySelect').onchange=e=>{activeCity=Number(e.target.value);cityDraftId=null;renderPanel();};$('panel').querySelectorAll('[data-local]').forEach(el=>el.oninput=()=>{cityDraft[el.dataset.local]=Number(el.value);$('lo-'+el.dataset.local).textContent=el.value;});if($('applyLocal'))$('applyLocal').onclick=applyCityManagement;$('panel').querySelectorAll('[data-queue]').forEach(b=>b.onclick=()=>queueAction(b.dataset.queue,Number(b.dataset.index)));if($('tradePartner'))$('tradePartner').onchange=e=>tradePartner=e.target.value;if($('signTrade'))$('signTrade').onclick=()=>signTrade($('tradePartner').value,$('tradeDirection').value,$('tradeGood').value,Number($('tradeShare').value));$('panel').querySelectorAll('[data-cancel-trade]').forEach(b=>b.onclick=()=>cancelTrade(Number(b.dataset.cancelTrade)));if($('outputMeasure'))$('outputMeasure').onchange=e=>changeOutput(e.target.value);if($('doctrine'))$('doctrine').onchange=e=>{if(current().id===S.player&&DOCTRINES[e.target.value]){current().doctrine=e.target.value;toast('Doctrine changed.');}};if($('monthSeconds'))$('monthSeconds').onchange=e=>{const seconds=Number(e.target.value);if(Number.isInteger(seconds)&&seconds>=4&&seconds<=30){S.options.monthSeconds=seconds;if(playing)startPause(true);toast('Simulation speed updated.');}else{e.target.value=S.options.monthSeconds;toast('Choose 4–30 seconds per month.');}};};
const v4RenderTickV5=renderTick;
renderTick=function(){if(['INPUT','SELECT','TEXTAREA'].includes(document.activeElement?.tagName)){renderStats();renderEvents();if(view==='trade'&&$('tradeSummary'))$('tradeSummary').innerHTML=tradeSummary(player());return;}v4RenderTickV5();};
const v4ProgramStatusV5=programStatus,v4ProgramHeadlineV5=programHeadline;
programStatus=function(key,n){if(['safety','enterprise'].includes(key))return PROGRAMS.find(p=>p.key===key).keys.map(k=>metric(POLICY.find(x=>x.key===k).name,n.policies[k].toFixed(1))).join('')+(key==='safety'?metric('Simulated national crime pressure',n.crime.toFixed(1)+' / 100'):'');return v4ProgramStatusV5(key,n);};
programHeadline=function(key,n){if(key==='safety')return 'Simulated crime pressure '+n.crime.toFixed(0)+'/100 · justice '+n.policies.justice.toFixed(0)+'/100';if(key==='enterprise')return 'Trade openness '+n.policies.trade.toFixed(0)+'/100 · tariff '+n.policies.tariff.toFixed(0)+'%';return v4ProgramHeadlineV5(key,n);};
const v4GuideV5=showGuide;
showGuide=function(){v4GuideV5();$('modalContent').insertAdjacentHTML('afterbegin','<div class="block"><h3>Sovereign V5 · Cities, safety & trade</h3><p>All 193 UN member states, both observer states, and the existing territories have real named settlements or districts. Small added states use map markers. Place names are geographical references; local output, population, crime and service indices are game assumptions. Some entries are villages, quarters or research stations rather than separate cities.</p><p><b>Local government:</b> set police, prevention, services, maintenance, density and tax supplements. Crime reacts over time to employment, housing, justice and prevention. Queues hold three projects, with one unpaused project building at a time. Pause, prioritize or cancel future work. Infrastructure needs recurring upkeep.</p><p><b>Trade:</b> create food, energy, manufactured-goods and technology contracts. Each delivery creates identical pre-tariff exports and imports. Commodity and demand limits prevent overselling; sanctions and war suspend routes. Tariffs are government revenue, while private export sales are not. National production profiles are scenario assumptions.</p><p><b>Military:</b> distribute the existing defense budget across army, navy and air forces. The shares must total 100%. Training and logistics improve capability while leaving fewer resources for equipment. Mobilization adds spending and reduces civilian output. Doctrine affects the abstract war model; no nuclear forces are modeled.</p><p><b>GDP and NMP:</b> the Economy selector reports GDP or an approximate Net Material Product: GDP × material-production share × (1 − material depreciation). It includes modeled material distribution services but omits most nonmaterial services and historical accounting adjustments. Changing the display never creates money or changes GDP, debt or tax bases.</p><p><b>Speed and flags:</b> 1× defaults to eight seconds per month. Change timing or choose a national/custom flag in Sandbox. V1–V4 saves migrate; named-city projects are preserved, including when place spelling has changed.</p><p>References: <a target="_blank" rel="noopener noreferrer" href="https://www.elibrary.imf.org/display/book/9781557753045/ch02.xml">IMF explanation of Net Material Product, Box 1</a>; <a target="_blank" rel="noopener noreferrer" href="https://www.oecd.org/en/data/indicators/nominal-gross-domestic-product-gdp.html">OECD GDP definition</a>; <a target="_blank" rel="noopener noreferrer" href="https://tradecosts.wto.org/">WTO trade costs</a>; <a target="_blank" rel="noopener noreferrer" href="https://www.unodc.org/documents/data-and-analysis/Crime-statistics/Manual_on_Victimization_surveys_2009_web.pdf">UNODC crime measurement guidance</a>. These inform concepts, not calibrated game coefficients.</p></div>');};
const v4MigrateV5=migrateSave;
migrateSave=function(s){s=v4MigrateV5(s);if(s?.version!==4)return s;const existing=new Set(s.nations.map(n=>n.id));for(const row of SEEDS)if(ADDED_IDS.has(row[0])&&!existing.has(row[0])){const n=seedNation(row);ensureV4(n);s.nations.push(n);}for(const n of s.nations)upgradeNationV5(n);s.version=5;s.options.outputMeasure='gdp';s.options.monthSeconds=8;s.tradeDeals=[];s.tradeFlows=[];s.nextTradeId=1;return s;};
